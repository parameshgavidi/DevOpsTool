================================================================================
FIX — git push rejected: "push declined due to repository rule violations"
Secret scanning found "-- USPS Password --"
Repo: github.usps.gov/appdev/gss-utilities  branch: devsecops-app
Commit: 522e4f61beb787fdd6a991706edb8af422d46e4
================================================================================

WHAT THE ERROR MEANS
--------------------
GitHub blocked your push because secret scanning thinks a password is inside
the commit. It is NOT a git credential / login problem.


WHAT WAS FLAGGED (from your PowerShell output)
----------------------------------------------
1) GssDevOpsAutomationTool/gss-devsecops-tool-specs.html  (line 293)
   → CHECK THIS. May be a real password / connection string in the HTML.

2) GssDevOpsAutomationTool/Resources/Fonts/OpenSans-Regular.ttf
   (lines 683, 697, 710, 723, 726)
   → Almost always a FALSE POSITIVE.
     .ttf is a binary font. Scanners mis-read binary bytes as a "password".


================================================================================
STEP 1 — Check the HTML (do this first)
================================================================================
In the repo F:\GSS\AppDev\gss-utilities open:

  GssDevOpsAutomationTool\gss-devsecops-tool-specs.html

Go to line 293. Look for a real password, passphrase, connection string,
API key, or "Password=...".

A) If it is a REAL secret
   - Delete / redact it (replace with placeholder like YOUR_PASSWORD_HERE)
   - Do NOT commit real passwords to git
   - Then follow STEP 3 (rewrite the commit) — unblocking a real password is wrong

B) If it is NOT a secret (docs example, fake value, redacted already)
   - You can use the unblock links (STEP 2) OR still redact to be safe


================================================================================
STEP 2 — Unblock FALSE POSITIVES (fonts + verified-safe HTML)
================================================================================
Your push output already gave unblock URLs. In a browser (signed into
github.usps.gov), open the unblock-secret links from the error, approve
the false positive for:

  - OpenSans-Regular.ttf  (safe to unblock — binary font)
  - HTML only if you confirmed line 293 is NOT a real password

Then push again:

  git push -u origin devsecops-app

If your org requires a reason, say:
  "False positive: OpenSans-Regular.ttf is a binary font file."


================================================================================
STEP 3 — If HTML had a real secret: remove it from the COMMIT (required)
================================================================================
Editing the file is not enough if the secret is already inside commit
522e4f6. Secret scanning reads the commit contents.

3a) Fix the file (remove/redact the password), save.

3b) See if that commit is your latest commit on the branch:

  git log -1 --oneline
  git status

3c) If 522e4f6 is the LATEST commit and NOT pushed yet (your case — push failed):

  git add GssDevOpsAutomationTool/gss-devsecops-tool-specs.html
  git commit --amend --no-edit

  # Optional: stop tracking binary fonts if you do not need them in git
  # (or keep fonts and unblock as false positive — preferred)

  git push -u origin devsecops-app

3d) If secret exists in OLDER commits too:

  # Find commits touching the file
  git log --oneline -- GssDevOpsAutomationTool/gss-devsecops-tool-specs.html

  Then either:
  - interactive rebase and edit those commits, or
  - ask a repo admin to help purge history

  After history rewrite:

  git push -u origin devsecops-app


================================================================================
STEP 4 — Optional: keep fonts but avoid future false positives
================================================================================
Preferred: keep OpenSans-Regular.ttf and unblock as false positive.

Alternative (only if fonts are not required in the repo):

  # Add to .gitignore
  GssDevOpsAutomationTool/Resources/Fonts/*.ttf

  git rm --cached GssDevOpsAutomationTool/Resources/Fonts/OpenSans-Regular.ttf
  git commit -m "Stop tracking font binaries that trigger secret scanning"
  git push -u origin devsecops-app

Do this only after the HTML secret is cleaned (or unblocked if false positive).


================================================================================
QUICK DECISION GUIDE
================================================================================
Font .ttf flagged?
  → FALSE POSITIVE → use unblock URL (STEP 2)

HTML line 293 has a real password?
  → REMOVE/REDACT → amend commit (STEP 3) → push
  → Do NOT unblock a real password

HTML line 293 is fake/docs only?
  → unblock OR redact → push


================================================================================
CHECKLIST
================================================================================
[ ] Opened gss-devsecops-tool-specs.html line 293
[ ] Real secret removed/redacted (if any)
[ ] Font flagged → unblock as false positive (or stop tracking .ttf)
[ ] Amended commit so secret is gone from history (if it was real)
[ ] git push -u origin devsecops-app succeeds


================================================================================
END
================================================================================
