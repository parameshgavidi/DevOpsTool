================================================================================
SUPERSEDED for From/To source — use folder-based guide instead
================================================================================

STATUS
------
From/To are UI-only (not appsettings). Load them as GIT branches again.

IF DROPDOWN SHOWS: obj / Schema Objects / Scripts
→ You are loading folders. FIX:
    docs/fixes/db-rollup-git-branches-not-folders.txt

Full DB get-latest + remove From/To config:
    docs/fixes/db-scripts-folders-from-to-get-latest.txt
  (From/To loading section superseded by git-branches-not-folders)


OLD CONTENT BELOW — DO NOT USE FOR NEW WORK
================================================================================

See:
  docs/fixes/db-rollup-git-branches-not-folders.txt


================================================================================
END
================================================================================
