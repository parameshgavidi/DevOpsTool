================================================================================
DB ROLLUP From/To — current pointer
================================================================================

STATUS
------
Two different paths — do not mix them:

1) dbScriptSource = F:\GSS\AppDev\gss-database
   → Get latest gss-database only (build:gitLatestCommand)

2) dbRollupBranchRoot = F:\GSS\DBRollupScript
   → From Branch / To Branch folders live here
   → On load + dropdown change: create folder OR git pull that branch
   → From must be < To
   → Paste guide:
      docs/fixes/db-rollup-from-to-folder-create-getlatest.txt   ← USE THIS

3) Dropdown options = GIT branches (GSS* from RepoRoot / gss-root)
   → docs/fixes/db-rollup-git-branches-not-folders.txt


================================================================================
END
================================================================================
