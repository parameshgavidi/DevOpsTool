================================================================================
SUPERSEDED for From/To source — use folder-based guide instead
================================================================================

STATUS
------
From/To no longer come from git remotes or appsettings dbRollupFromBranch /
dbRollupToBranch.

USE THIS FILE INSTEAD:

    docs/fixes/db-scripts-folders-from-to-get-latest.txt

That guide:
  - Script Source = F:\GSS\AppDev\gss-database\Scripts (dbScriptSource)
  - Loads folder names (8.4 … 9.2, Prod) into To Branch dropdown
  - To = latest by default; From = previous (readonly)
  - Removes dbRollupFromBranch / dbRollupToBranch from appsettings + model load
  - Get latest also pulls DB project with build-level gitLatestCommand


OLD CONTENT BELOW (git branch -r From/To) — DO NOT USE FOR NEW WORK
================================================================================

See git history if you need the previous git-remote dropdown steps.
For current paste-ready steps, open:

    docs/fixes/db-scripts-folders-from-to-get-latest.txt


================================================================================
END
================================================================================
