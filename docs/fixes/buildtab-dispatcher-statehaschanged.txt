================================================================================
Build All → InvalidOperationException: thread not associated with Dispatcher
Fix HandleBuildSuccessChanged in PARENT tabs (NOT BuildTab)
================================================================================

SYMPTOM
-------
Click Build All (or Build). Debugger stops on StateHasChanged():

  System.InvalidOperationException:
  The current thread is not associated with the Dispatcher.
  Use InvokeAsync() to switch execution to the Dispatcher when triggering
  rendering or component state.


WHERE THE METHOD ACTUALLY LIVES
-------------------------------
HandleBuildSuccessChanged is NOT in BuildTab.razor.

Find-in-solution for "HandleBuildSuccessChanged" shows hits like:

  DeployTab.razor
    ~line 74:  <BuildTab OnBuildSuccessChanged="HandleBuildSuccessChanged" />
    ~line 150: private Task HandleBuildSuccessChanged(bool success)

  DeploymentTool.razor
    ~line 38:  <DeployTab ... OnBuildSuccessChanged="HandleBuildSuccessChanged" />
    ~line 180: private Task HandleBuildSuccessChanged(bool hasSuccessfulBuild)

BuildTab only RAISES the event:

  await OnBuildSuccessChanged.InvokeAsync(true);

Parents HANDLE it. Fix those handlers.


ROOT CAUSE
----------
Build finishes on a background thread → OnBuildSuccessChanged fires → parent
HandleBuildSuccessChanged runs off the UI thread → bare StateHasChanged() throws.


================================================================================
THE ONLY REQUIRED CHANGE
================================================================================
In EACH HandleBuildSuccessChanged (DeployTab + DeploymentTool):

  KEEP your existing method body exactly as it is.
  Do NOT add any new variables (no HasSuccessfulBuild needed).
  ONLY change how you call StateHasChanged.


BEFORE (broken):

    private Task HandleBuildSuccessChanged(bool success)
    {
        // ... whatever you already have here ...
        StateHasChanged();
        return Task.CompletedTask;
    }

AFTER (fixed) — Option A:

    private Task HandleBuildSuccessChanged(bool success)
    {
        // ... whatever you already have here (unchanged) ...
        return InvokeAsync(StateHasChanged);
    }

AFTER (fixed) — Option B (if the method already uses async/await):

    private async Task HandleBuildSuccessChanged(bool success)
    {
        // ... whatever you already have here (unchanged) ...
        await InvokeAsync(StateHasChanged);
    }

Parameter name may be `success` or `hasSuccessfulBuild` — match YOUR file.
Do the same edit in BOTH DeployTab.razor and DeploymentTool.razor.


EXAMPLES (illustrative — keep YOUR real body)
---------------------------------------------
If DeployTab currently looks like:

    private Task HandleBuildSuccessChanged(bool success)
    {
        return OnBuildSuccessChanged.InvokeAsync(success);
    }

and somehow still hits StateHasChanged elsewhere, fix THAT StateHasChanged.
If it currently has StateHasChanged:

    private Task HandleBuildSuccessChanged(bool success)
    {
        // existing lines stay
        return InvokeAsync(StateHasChanged);
    }

If DeploymentTool currently looks like:

    private Task HandleBuildSuccessChanged(bool hasSuccessfulBuild)
    {
        // existing lines stay — do not invent new fields
        return InvokeAsync(StateHasChanged);
    }


Do NOT keep:

    StateHasChanged();
    return Task.CompletedTask;


--------------------------------------------------------------------------------
Wiring — leave alone
--------------------------------------------------------------------------------
  DeployTab:       <BuildTab OnBuildSuccessChanged="HandleBuildSuccessChanged" />
  DeploymentTool:  <DeployTab ... OnBuildSuccessChanged="HandleBuildSuccessChanged" />


--------------------------------------------------------------------------------
Checklist
--------------------------------------------------------------------------------
[ ] DeployTab.HandleBuildSuccessChanged → InvokeAsync(StateHasChanged)
[ ] DeploymentTool.HandleBuildSuccessChanged → InvokeAsync(StateHasChanged)
[ ] No new HasSuccessfulBuild (or other) variable added
[ ] Click Build All — no Dispatcher InvalidOperationException
[ ] UI still updates after builds finish


--------------------------------------------------------------------------------
Quick test
--------------------------------------------------------------------------------
1. Run under debugger
2. Click Build All
3. Expect: no Exception on StateHasChanged
4. Expect: status / log UI still refreshes
