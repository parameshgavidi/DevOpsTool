================================================================================
DeployTab error on OnBuildSuccessChanged.HasDelegate + Build All Dispatcher crash
================================================================================

WHAT YOU ARE SEEING IN DeployTab.razor (~line 150)
--------------------------------------------------
    private Task HandleBuildSuccessChanged(bool success)
    {
        return OnBuildSuccessChanged.HasDelegate
            ? OnBuildSuccessChanged.InvokeAsync(success)
            : Task.CompletedTask;
    }

Red squiggles on OnBuildSuccessChanged / HasDelegate mean DeployTab does not
have a proper EventCallback parameter named OnBuildSuccessChanged.


--------------------------------------------------------------------------------
1) DeployTab.razor — ADD the missing Parameter
--------------------------------------------------------------------------------
Near your other [Parameter] properties in DeployTab.razor @code, ADD:

    [Parameter]
    public EventCallback<bool> OnBuildSuccessChanged { get; set; }

That is required because DeploymentTool.razor already passes it:

    <DeployTab ... OnBuildSuccessChanged="HandleBuildSuccessChanged" />

After adding the Parameter, HasDelegate and InvokeAsync compile.


--------------------------------------------------------------------------------
2) DeployTab.razor — KEEP your forwarder method as-is
--------------------------------------------------------------------------------
Your current method is fine. Do NOT change it to StateHasChanged.
Do NOT add HasSuccessfulBuild.

    private Task HandleBuildSuccessChanged(bool success)
    {
        return OnBuildSuccessChanged.HasDelegate
            ? OnBuildSuccessChanged.InvokeAsync(success)
            : Task.CompletedTask;
    }

This only forwards the bool up to DeploymentTool.


--------------------------------------------------------------------------------
3) DeploymentTool.razor — FIX the Dispatcher crash HERE
--------------------------------------------------------------------------------
The original runtime error (thread not associated with Dispatcher) is in
DeploymentTool.HandleBuildSuccessChanged (~line 180), which typically has:

    private Task HandleBuildSuccessChanged(bool hasSuccessfulBuild)
    {
        // ... your existing lines ...
        StateHasChanged();              // ← throws off UI thread
        return Task.CompletedTask;
    }

REPLACE only the re-render part:

    private Task HandleBuildSuccessChanged(bool hasSuccessfulBuild)
    {
        // ... keep your existing lines unchanged ...
        return InvokeAsync(StateHasChanged);
    }

Or if already async:

    private async Task HandleBuildSuccessChanged(bool hasSuccessfulBuild)
    {
        // ... keep your existing lines unchanged ...
        await InvokeAsync(StateHasChanged);
    }

Do NOT invent new variables. Keep whatever body you already have.
Only remove bare StateHasChanged() + Task.CompletedTask and use InvokeAsync.


--------------------------------------------------------------------------------
4) Call chain (for clarity)
--------------------------------------------------------------------------------
  BuildTab  →  OnBuildSuccessChanged.InvokeAsync(true)
       ↓
  DeployTab.HandleBuildSuccessChanged(success)
       ↓  forwards via Parameter OnBuildSuccessChanged
  DeploymentTool.HandleBuildSuccessChanged(...)
       ↓  must use InvokeAsync(StateHasChanged)


--------------------------------------------------------------------------------
5) Checklist
--------------------------------------------------------------------------------
[ ] DeployTab has: [Parameter] public EventCallback<bool> OnBuildSuccessChanged { get; set; }
[ ] DeployTab.HandleBuildSuccessChanged still only forwards (no StateHasChanged needed)
[ ] DeploymentTool.HandleBuildSuccessChanged uses InvokeAsync(StateHasChanged)
[ ] Red squiggles gone in DeployTab
[ ] Click Build All — no Dispatcher InvalidOperationException
