================================================================================
DeploymentTool.razor — paste this exact code (field + 2 methods)
================================================================================

Put the field with your other private fields in @code.
Replace SelectTab + HandleBuildSuccessChanged with the methods below.


    private bool HasSuccessfulBuild;

    private void SelectTab(string tabName)
    {
        if (!HasSuccessfulBuild && tabName != "BUILD") return;
        ActiveTab = tabName;
    }

    private Task HandleBuildSuccessChanged(bool hasSuccessfulBuild)
    {
        HasSuccessfulBuild = HasSuccessfulBuild || hasSuccessfulBuild;
        return InvokeAsync(StateHasChanged);
    }


DeployTab.razor — leave as-is (no HasSuccessfulBuild there):

    private Task HandleBuildSuccessChanged(bool success)
    {
        return OnBuildSuccessChanged.HasDelegate
            ? OnBuildSuccessChanged.InvokeAsync(success)
            : Task.CompletedTask;
    }

And DeployTab must also have this Parameter (if missing, add it):

    [Parameter]
    public EventCallback<bool> OnBuildSuccessChanged { get; set; }


If save still errors, copy the EXACT red error text (Error List).
Common causes:
  - field declared twice
  - field put outside @code
  - typo: HasSuccessfullBuild / hasSuccessfulBuild field name mismatch
